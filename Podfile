# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MiFaena' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MiFaena
  pod 'Firebase/Analytics'
  pod 'CSV.swift', '~> 2.4.3'
  pod 'SDWebImage', '~> 5.0'
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'
  pod 'Firebase/Storage' 
target 'MiFaenaTests' do
  inherit! :search_paths
  # Pods for testing 
  pod 'Firebase'
  end
target 'MiFaenaUITests' do
  inherit! :search_paths
  # Pods for testing
  pod 'Firebase'
  end
end
